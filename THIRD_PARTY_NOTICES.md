# Third Party Notices

TypeType Token depends on third-party packages listed in `package.json` and `bun.lock`.

| Package | License | Notes |
|---|---|---|
| `bgutils-js` | MIT | BotGuard / PO token challenge flow |
| `playwright` | Apache-2.0 | Browser automation runtime |
| `playwright-core` | Apache-2.0 | Playwright runtime dependency |

The Docker runtime image uses `mcr.microsoft.com/playwright:v1.59.1-noble`.
